# Benchmarks
Various benchmarks.
- Run `cargo bench` to execute benchmarks.
- Run `cargo run` (after running benchmarks) to generate plots.
- See docs to add / change benchmarks: https://bheisler.github.io/criterion.rs/book/criterion_rs.html
